{
  "name": "san-gaya-massage",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx firebase-tools deploy"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^5.4.2"
  }
}
